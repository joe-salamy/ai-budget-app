/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class",
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // In Tailwind v4, theme configuration is done via @theme in CSS
  // See src/index.css for color and design token definitions
};
